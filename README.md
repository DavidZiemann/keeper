# Keeper Mobile Safe Box
Application built using react & framework7 to use as a safe box.

## Build Scripts

Running the app:

```
npm install
npm start
```

To build your app for deployment, run:

```
npm run build
```
